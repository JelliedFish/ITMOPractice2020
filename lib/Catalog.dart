

import 'package:flutter/cupertino.dart';
import 'package:flutter/material.dart';

class MathCatalog extends StatelessWidget{

  Widget build(BuildContext context){

    return Scaffold(
      body: Center(
        child: Text(
          "Каталог Математики"
        ),
      ),

    );
  }
}


class RussianCatalog extends StatelessWidget{

  Widget build(BuildContext context){

    return Scaffold(
      body: Center(
        child: Text(
          "Каталог Русского"
        ),
      ),

    );
  }
}